# ミリしらソード — AI作業指針

ゲーム仕様に関わる変更を始める前に、必ず
[`docs/GAME_DESIGN.md`](docs/GAME_DESIGN.md) を最後まで読むこと。

## 変更時に守ること

- ゲームは見下ろし型・ターン制・マス単位移動の文字ブロックパズル。
- 方向入力1回につき、勇者の論理座標は最大1マスだけ移動させる。塞がれている場合は向きだけ変え、ターンを消費しない。
- 文字ブロックは通常1個だけ押せる。2個以上をまとめて押せるのは、正しい合成壁レシピを満たす場合だけ。
- 文字を未充填の溝へ押すと文字は消え、溝は歩行可能な床になる。
- 方向長押しは一定間隔の1マス移動とし、離した後に移動予約を残さない。
- 複数方向が押された場合は最後に押した方向を優先し、それを離したら残っている方向へ復帰する。
- 同方向の長押し移動も論理上は1ターンずつ解決し、敵や視線の判定を飛ばさない。
- ワールド状態、カメラ、描画、入力を分離する。
- ステージ全体を論理データとして保持し、Canvasにはカメラ内だけを描画する。
- 通常起動は13面から選択可能とし、初期選択は会議用15×15ステージにする。
- 各面は固定カメラで盤面全体と外周を表示する。
- 移動・当たり判定は整数マスを保ち、カメラの `view` だけは0.5マス単位を許可する。
- カメラの画角はコードへ直書きせず、ステージデータの `cameraAreas` で編集可能にする。
- ステージエディターの下書きは通常ゲームへ自動適用せず、`playtest=1` のときだけ読み込む。
- エディターのJSON入出力形式とゲームが読む `StageDefinition` の互換性を保つ。
- PCとスマートフォンの操作を同じ `GameControl` に変換する。
- 日本語斬りと英語斬りは切替式にせず、PCではJ／K、スマートフォンでは「あ」／「A」の独立した入力として扱う。
- 未決定のゲーム内容を勝手に補完しない。仮表示は明確に交換可能なものにする。
- UIは簡潔に保ち、過剰なグラデーション、カード、装飾、説明文を追加しない。
- 完了前に `npm.cmd run build` を実行する。
